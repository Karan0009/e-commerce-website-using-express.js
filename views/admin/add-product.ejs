<!-- this view is not used instead edit-product.ejs is being used
because this page and edit-product.ejs are kinda same and 
a generalized page can be created which is edit-product.ejs -->

<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/add-product.css" />
  </head>
  <body>
    <%- include('../includes/nav.ejs') %>
    <main>
      <form class="product-form" method="POST" action="/product">
        <div class="form-control">
          <label for="title">Title</label>
          <input type="text" name="title" id="title" />
        </div>
        <div class="form-control">
          <label for="imageUrl">Image Url</label>
          <input type="text" name="imageUrl" id="imageUrl" />
        </div>
        <div class="form-control">
          <label for="price">Price</label>
          <input type="number" name="price" id="price" step="0.01" />
        </div>
        <div class="form-control">
          <label for="description">Description</label>
          <textarea name="description" id="description" rows="5"></textarea>
        </div>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit">
          Add Product
        </button>
      </form>
    </main>
<%- include('../includes/end.ejs') %>